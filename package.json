{
  "name": "taskchain-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "blockchain"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "compile:contracts": "npm run compile --workspace=blockchain",
    "test:contracts": "npm run test --workspace=blockchain"
  }
}